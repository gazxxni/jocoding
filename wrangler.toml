# 프로젝트의 이름을 설정합니다. (Cloudflare 대시보드에 표시될 이름)
name = "jocoding"

# Cloudflare Workers 런타임의 호환성 날짜를 설정합니다. (오늘 날짜 권장)
compatibility_date = "2026-02-15"

# -----------------------------------------------------------------------
# [상황 1] HTML, React, Vue 등 정적 웹사이트를 배포하는 경우
# 빌드 결과물이 위치하는 폴더명을 지정해야 합니다. (예: dist, build, public 등)
# 아래 주석을 해제하고 실제 빌드 폴더명으로 수정하세요.
# -----------------------------------------------------------------------
# [site]
# bucket = "./dist" 


# -----------------------------------------------------------------------
# [상황 2] 서버리스 함수(Worker)를 배포하는 경우 (예: index.ts, worker.js)
# 워커 스크립트의 진입점(메인 파일) 경로를 지정해야 합니다.
# 아래 주석을 해제하고 실제 파일 경로로 수정하세요.
# -----------------------------------------------------------------------
# main = "src/index.ts"
